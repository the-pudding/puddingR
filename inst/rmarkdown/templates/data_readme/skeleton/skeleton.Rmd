---
title: 
author: 
date: 
output: 
  github_document:
    html_preview: true
params: 
  directory: paste0(getwd(), "/assets/data/open_data/intermediate/")
  
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

This folder contains all of the data used in The Pudding essay [My Awesome Essay](https://pudding.cool) published in February 2018.

This file was last updated: `r format(Sys.time(), '%B %d, %Y')`

`r print(params$directory)`

```{r}
md <- list.files(path = params$directory,
                   pattern = '*.md', 
                   recursive = T)
```


```{r}
purrr::walk(.x = md, .f = function(x){
  knitr::knit_child(paste0(getwd(), "/assets/data/open_data/intermediate/", x))
})
```


```{r echo = FALSE, results='asis'}


chunks <- paste0("```{r child = '", md, "' eval = TRUE}\n test \n ```\n")
 
cat(chunks, sep = '\n')
```
 
## Session Info

```{r session_info, include=TRUE, echo=TRUE, results='markup'}
devtools::session_info()
```

